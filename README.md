# Trang Web Chúc Mừng Sinh Nhật 💕

Một trang web sinh nhật đơn giản, nhẹ nhàng và lãng mạn.

## Cách sử dụng

1. **Thêm ảnh của người yêu:**
   - Đặt ảnh vào thư mục này với tên `photo.jpg`
   - Hoặc đổi tên ảnh khác trong file `index.html` (dòng `<img src="photo.jpg"...`)
   - Ảnh sẽ tự động cắt thành hình tròn

2. **Chỉnh sửa nội dung:**
   - Mở file `index.html`
   - Đổi tên trong thẻ `<h2>` (dòng "Em Yêu Của Anh")
   - Sửa lời chúc trong phần `<div class="message">`

3. **Thêm nhạc nền (tùy chọn):**
   - Đặt file nhạc MP3 với tên `happy-birthday.mp3` vào thư mục
   - Hoặc đổi tên file trong `index.html` (dòng `<source src="..."`)

4. **Chạy trang web:**
   - Mở file `index.html` bằng trình duyệt
   - Hoặc click đúp vào file `index.html`

## Tính năng

✨ Thiết kế tối giản, màu pastel nhẹ nhàng
📸 Hiển thị ảnh hình tròn với hiệu ứng hover
🕯️ Nến nhấp nháy trên bánh sinh nhật
❤️ Trái tim bay lơ lửng
🎈 Bóng bay animation
🎉 Click vào bánh để tạo hiệu ứng confetti
🎵 Nút bật/tắt nhạc nền
📱 Responsive trên mọi thiết bị

## Cấu trúc file

```
├── index.html      # File HTML chính
├── style.css       # File CSS (giao diện)
├── script.js       # File JavaScript (hiệu ứng)
├── photo.jpg       # Ảnh của người yêu (bạn cần thêm)
└── happy-birthday.mp3  # Nhạc nền (tùy chọn)
```

## Tùy chỉnh màu sắc

Mở file `style.css` và tìm các dòng sau để đổi màu:

- `#ff6b9d` - Màu hồng chính
- `#c44569` - Màu hồng đậm
- `#ffecd2`, `#fcb69f` - Màu nền gradient

Chúc bạn có một ngày sinh nhật thật đặc biệt! 💕
